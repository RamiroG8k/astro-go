---
import Stack, { type Props as StackProps } from './_Stack.astro';
export type Props = Omit<StackProps, 'direction'>;
const { ...attrs } = Astro.props;
---

<Stack direction="row" {...attrs}>
    <slot />
</Stack>